<Page
    x:Class="WhaleComics_2._0.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WhaleComics_2._0"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="News: RSS If We Can" FontSize="100"
                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal"
                    Name="ItemOfTheDayStackPanel">
            <Image />
            <Image />
            <TextBlock Name="TestTextBox"/>
            <Button />
        </StackPanel>
        <StackPanel Grid.Row="2">
            <TextBlock/>
            <StackPanel Orientation="Horizontal">
                <Button Name="FacebookButton" />
                <Button Name="InstegramButton" />
                <Button Name="TwitterButton" />
                <TextBlock />
                <HyperlinkButton />
            </StackPanel>
        </StackPanel>

    </Grid>
</Page>
